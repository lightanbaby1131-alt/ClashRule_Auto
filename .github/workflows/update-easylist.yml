name: Update EasyList Clash List

on:
  schedule:
    - cron: "0 2 * * *"   # 每天 UTC 2 点（北京时间 10 点）
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install requests

      - name: Convert EasyList to Clash list
        env:
          OUTPUT_PATH: "name: Update EasyList Clash List

on:
  schedule:
    - cron: "0 2 * * *"   # 每天 UTC 2 点（北京时间 10 点）
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install requests

      - name: Convert EasyList to Clash list
        env:
          OUTPUT_PATH: "Clash/Ruleset/AD/easylist.list"   # ←←← 你在这里填写路径
        run: |
          python scripts/convert_easylist.py "$OUTPUT_PATH"

      - name: Commit and push if changed
        run: |
          if git status --porcelain | grep -q ".list"; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add .
            git commit -m "chore: update easylist.list"
            git push
          else
            echo "No changes detected"
          fi
"   # ←←← 你在这里填写路径
        run: |
          python scripts/convert_easylist.py "$OUTPUT_PATH"

      - name: Commit and push if changed
        run: |
          if git status --porcelain | grep -q ".list"; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add .
            git commit -m "chore: update easylist.list"
            git push
          else
            echo "No changes detected"
          fi
