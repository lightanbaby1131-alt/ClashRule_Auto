name: Update Foreign AI Rules

permissions:
  contents: write

on:
  schedule:
    - cron: "0 22 * * *"   # 每天 UTC 22:00（北京时间 06:00）
  workflow_dispatch:

jobs:
  update-ai-rules:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install requests

      - name: Generate Foreign AI rules
        run: |
          python scripts/convert_ai_rules.py

      - name: Pull latest changes
        run: |
          git pull --rebase origin main || true

      - name: Commit and push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          if git status --porcelain | grep .; then
            git add Clash/Ruleset/AI/ForeignAI.list
            git add .github/tmp/*.txt
            git commit -m "国外AI域名自动更新"
            git push origin main
          else
            echo "No changes."
          fi
